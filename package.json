{
  "name": "@wallaround/root",
  "version": "1.0.0",
  "description": "Wallaround Take Home Assignment",
  "scripts": {
    "format": "nx affected -t format",
    "format:all": "nx run-many --target=format --all",
    "test": "nx affected -t test -- --silent --watch=false",
    "test:coverage": "nx affected -t test:coverage -- --silent --watch=false",
    "type-check": "nx affected -t type-check"
  },
  "keywords": [],
  "author": "Moses Adebayo",
  "license": "ISC",
  "workspaces": [
    "libs/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20.17.0",
    "npm": ">=10.2.3"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^27.0.0",
    "nx": "^19.8.14",
    "prettier": "^3.5.1",
    "vitest": "^3.2.4"
  }
}
